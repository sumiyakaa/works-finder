# Public Assets

React 側で配信する静的素材を用途ごとに整理しています。

- `images/meta/`
  favicon、apple-touch-icon、OGP 画像、manifest アイコン
- `images/works/`
  `src/data/works.ts` が参照する作品サムネイル一式
- `images/case-studies/works-finder/`
  Works Finder case study の進化履歴で使う代表画像

`Vanilla` は参照元のまま保持し、このフォルダには React 側で今後確実に使う素材だけをコピーしています。
